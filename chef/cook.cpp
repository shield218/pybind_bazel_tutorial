// complicated_project/chef/cook.cpp
#include "cook.h"

namespace chef {
    namespace cook {
        // Implements bake by simply adding 10 to the input
        int bake(int dough) {
            return dough + 10;
        }
    }
}
